import { Personality } from '../../../../shared/interfaces';

export class Delver extends Personality {
  static description = 'You will only delve deeper into the dark dungeons.';
  static statMultipliers = { };
  static toggleOff = [];
}
