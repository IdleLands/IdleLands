import { Personality } from '../../../../shared/interfaces';

export class Autoscender extends Personality {
  static description = 'You will always ascend automatically when reaching max level.';
  static statMultipliers = { };
  static toggleOff = [];
}
